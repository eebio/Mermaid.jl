# API

## Mermaid

```@autodocs
Modules = [Mermaid]
Order   = [:function, :type]
```

## Extensions

```@autodocs
Modules = [Base.get_extension(Mermaid, :AgentsExt), Base.get_extension(Mermaid, :DiffEqExt), Base.get_extension(Mermaid, :MethodOfLinesExt), Base.get_extension(Mermaid, :SurrogatesExt)]
Order   = [:function, :type]
```
